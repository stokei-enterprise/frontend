{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "build": "npx lerna run build",
    "start": "npx lerna run start --stream",
    "dev": "npx lerna run dev --parallel",
    "dev:auth": "npx lerna run dev --scope @stokei/auth",
    "dev:core": "npx lerna run dev --scope @stokei/core",
    "dev:dashboard": "npx lerna run dev --scope @stokei/dashboard",
    "dev:ui": "npx lerna run dev --scope @stokei/ui",
    "check-updates": "npx lerna exec npm run check-updates",
    "check-updates-deploy": "npx lerna exec npm run check-updates-deploy"
  },
  "devDependencies": {
    "create-react-library": "^3.1.1",
    "lerna": "^4.0.0",
    "typescript": "^4.4.3"
  }
}
